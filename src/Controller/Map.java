package Controller;

import com.sun.javafx.scene.traversal.Direction;

import javax.swing.*;

public class Map {
   /* int mapX;
    int mapY;

    Player player;

    JFrame frame;

    Map(JFrame frame) {
        this.frame = frame;
        player = new Player(frame);
    }

    public void border() {
        int stop = 4;

        if ((player.getxCoordinate()) > (586)) {
            stop = 0;
        }
        if ((player.getxCoordinate() < 150)) {
            stop = 1;
        }

        if ((player.getyCoordinate()  > 1116)) {
            stop = 2;
        }
        if ((player.getyCoordinate()  < 125)) {
            stop = 3;
        }
        switch (stop) {
            case 0:
                player.playerDirection = Direction.LEFT;
                break;

            case 1:
                player.playerDirection = Direction.RIGHT;
                break;

            case 2:
                player.playerDirection = Direction.UP;
                break;

            case 3:
                player.playerDirection = Direction.DOWN;
                break;
            default:
                break;
        }
    }

    public int getMapHihgBorderX() {
        return mapX;
    }

    public int getMapLowBorderX() {
        return mapX;
    }

    public int getMapHighBorderY() {
        return mapY;
    }

    public int getMapLowBorderY() {
        return mapY;
    }
    public int getCentre(){

        return mapX;
    }*/
}
